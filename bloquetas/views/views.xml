<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="view_tree_propiedades">
      <field name="name">view_tree_propiedades</field>
      <field name="model">bloquetas.propiedades</field>
      <field name="arch" type="xml">
        <tree>
          <field name="nombre"/>
          <field name="medidas"/>
          <field name="cantidad"/>
          <field name="presupuesto"/>
          <field name="tipo"/>
          <field name="fecha"/>
        </tree>
      </field>
    </record>
    <record id="view_form_propiedades" model="ir.ui.view">
      <field name="name">view_form_propiedades</field>
      <field name="model">bloquetas.propiedades</field>
      <field name="arch" type="xml"/>
        <form>
          <sheet>
            <group name="grupo 1" string="Grupo 1">
              <field name="nombre" string="Tu nombre"/>
              <field name="medidas" string="Tu medida"/>
              <field name="cantidad" string="Tu cantidad"/>
            </group>
            <group name="grupo 2" string="Grupo 2">
              <field name="presupuesto" string="Tu presupuesto"/>
              <field name="tipo" string="Tu tipo"/>
              <field name="fecha" string="Tu fecha"/>
            </group>
          </sheet>
        </form>
    </record>


    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="action_view_propiedades">
      <field name="name">PROPIEDADES</field>
      <field name="res_model">bloquetas.propiedades</field>
      <field name="view_mode">tree,form</field>
    </record>


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="bloquetas.action_server">
      <field name="name">bloquetas server</field>
      <field name="model_id" ref="model_bloquetas_bloquetas"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->

    <menuitem id="menu_propiedades" action="bloquetas.action_view_propiedades"/>

    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="bloquetas.menu_1" parent="bloquetas.menu_root"/>
    <menuitem name="Menu 2" id="bloquetas.menu_2" parent="bloquetas.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="bloquetas.menu_1_list" parent="bloquetas.menu_1"
              action="bloquetas.action_window"/>
    <menuitem name="Server to list" id="bloquetas" parent="bloquetas.menu_2"
              action="bloquetas.action_server"/>
-->
  </data>
</odoo>